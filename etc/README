Sundry system support files
==============================================================================

== init.d script
1. copy meerkatd to /etc/init.d/
2. enable:
    update-rc.d meerkatd defaults

to remove:
    update-rc.d meerkatd remove


== lighttpd config script
1. copy lighttpd.conf to /etc/lighttpd/lighttpd.conf
